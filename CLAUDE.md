# TradingCat

## Git 工作流

- SSH 推送 GitHub 经常超时断开，失败后自动重试，全部失败则报告未推送的提交数

## 测试

- 修改代码后运行测试套件
- Python 版本要求 3.11+，先确认版本再跑

## 大文件写入

- 单次写入超过 500 行时工具可能失败，改用拆分写入或 Bash 分段写入；同一操作连续失败 3 次后停止并报告

## 工作目录

- 多文件编辑前用 `pwd` 和 `git rev-parse --show-toplevel` 确认当前目录；worktree 环境下确认路径正确
